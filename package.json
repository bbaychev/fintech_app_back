{
    "name": "fintech_app_back",
    "version": "1.0.0",
    "dependencies": {
        "express": "^4.13.4",
        "mysql": "^2.10.2"
    },
    "scripts": {
        "start": "node server.js",
    },
    "heroku-postbuild": "cd client && npm install && npm install --only=dev --no-shrinkwrap && npm run build",
    "engines": "10.13.0"
}
